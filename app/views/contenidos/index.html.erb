 
<div class="container">
<h1 class="m-3"><%= $tema.descripcion %> - Contenidos <span class="h5"><%= link_to 'Volver', asignaturas_temas_path($tema.asignatura_id) %></span></h1> 
<table class="table table-dark">
	<thead> 
		<tr> 
			<th>Id</th>
			<th>Descripción</th>
			<th colspan=2 class="text-center">
				<%= link_to 'Nuevo', contenidos_nuevo_path, :class => "btn btn-primary" %>

			</th>
		</tr> 
	</thead> 
	<tbody> 
		<% @lista.each do |item|  %> 
		<tr> 
			<td>
				<%= item.id %>

			</td>
			<td>
				<%=item.descripcion %>

			</td>
			<td class="text-center">
				<%= link_to 'Editar',contenidos_editar_path(item.id), :class => "btn btn-success" %>

			</td>
			<td class="text-center"><%= link_to 'Borrar',contenidos_borrar_path(item.id),:class => "btn btn-danger", method: :delete %>

			</td>

		</tr>   
		<% end %>
	</tbody>
</table>
</div>