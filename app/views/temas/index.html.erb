<div class="container">
<h1 class="m-3"><%= $asignatura.ASIGN_NOMBRE %> - Temas</h1>
<table class="table table-dark"><thead>
<tr>
    <th>Id</th><th>Descripción</th>
    <th class="text-center"><%= link_to 'Nuevo', temas_nuevo_path, :class => "btn btn-primary" %></th>
    <th class="text-center"><%= link_to 'Nuevo Contenido', contenidos_nuevo_path, :class => "btn btn-primary" %></th>
    <th class="text-center">Ver Contenidos</th>
</tr>
</thead>
<tbody>
<%  @lista.each do |item|  %>
    <tr>
    <td><%= item.id %></td>
    <td><%= item.descripcion %></td>
    <td class="text-center"><%= link_to 'Editar', temas_editar_path(item.id), :class => "btn btn-success" %></td>
    <td class="text-center"><%= link_to 'Borrar', temas_borrar_path(item.id), :class => "btn btn-danger", method: :put %></td>
    <td class="text-center"><%= link_to 'Contenidos', temas_contenidos_path(item.id), :class => "btn btn-primary" %></td>
    </tr>   
<% end %>
</tbody>
</table>
</div>
